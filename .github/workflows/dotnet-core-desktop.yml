name: Build and Release

on:
  push:
    tags: 
      - "v*"
    branches:
      - master

jobs:
  build:
    runs-on: windows-latest

    steps:
    - uses: actions/checkout@v1
    
    - name: Setup MSBuild
      uses: microsoft/setup-msbuild@v1

    - name: Build App
      run: msbuild.exe SophiApp/SophiApp.sln /nologo /nr:false /p:DeployOnBuild=true /p:DeployDefaultTarget=WebPublish /p:WebPublishMethod=FileSystem /p:DeleteExistingFiles=True /p:platform="Any CPU" /p:configuration="Release" /p:PublishUrl="SophiApp"
    
    - name: Create Archive
      run: Compress-Archive -Path "SophiApp\SophiApp\bin\Release\*.exe" -DestinationPath "SophiApp\SophiApp\bin\Release\SophiApp.zip"
    
    - name: Set Variables
      id: vars
      run: echo "::set-output name=sha_short::$(git rev-parse --short HEAD)"
     
    - name: Create Release
      id: create_release
      uses: actions/create-release@v1
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      with:
        tag_name: version_${{ steps.vars.outputs.sha_short }}
        release_name: SophiApp
        draft: false
        prerelease: false
    
    - name: Update Release Asset
      id: upload-release-asset
      uses: actions/upload-release-asset@v1
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      with:
        upload_url: ${{ steps.create_release.outputs.upload_url }}  # This pulls from the CREATE RELEASE step above, referencing it's ID to get its outputs object, which include a `upload_url`. See this blog post for more info: https://jasonet.co/posts/new-features-of-github-actions/#passing-data-to-future-steps
        asset_path: 'SophiApp\SophiApp\bin\Release\SophiApp.zip'
        asset_name: 'SophiApp.zip'
        asset_content_type: application/zip
        asset_label: 'SophiApp.zip'
