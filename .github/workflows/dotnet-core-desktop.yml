name: Build and Release

on:
  schedule:
    - cron: '0 16 * * *'
  
jobs:
  build:
    runs-on: windows-latest

    steps:
    - uses: actions/checkout@v1
    
    - name: Set Variables
      id: vars
      run: echo "::set-output name=build_ver::$("1.{0}"-f (Get-Date -Format 'MMdd'))"
    
    - name: Setup MSBuild
      uses: microsoft/setup-msbuild@v1

    - name: Build App
      run: msbuild.exe SophiApp/SophiApp.sln /t:SophiAppCE /nologo /nr:false /p:DeployOnBuild=true /p:DeployDefaultTarget=WebPublish /p:WebPublishMethod=FileSystem /p:DeleteExistingFiles=True /p:platform="Any CPU" /p:configuration="Release" /p:PublishUrl="SophiAppCE"
      
    - name: Add Scripts Folder
      run: Expand-Archive -Path "PSScripts.zip" -DestinationPath "SophiApp\SophiAppCE\bin\Release"
    
    - name: Create Archive
      run: Compress-Archive -Path "SophiApp\SophiAppCE\bin\Release\*" -DestinationPath "SophiApp\SophiAppCE\bin\Release\SophiAppCE.zip"
     
    - name: Create Release
      id: create_release
      uses: actions/create-release@v1
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      with:
        tag_name: version_${{ steps.vars.outputs.build_ver }}
        release_name: SophiAppCE
        draft: false
        prerelease: false
    
    - name: Update Release Asset
      id: upload-release-asset
      uses: actions/upload-release-asset@v1
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      with:
        upload_url: ${{ steps.create_release.outputs.upload_url }}  # This pulls from the CREATE RELEASE step above, referencing it's ID to get its outputs object, which include a `upload_url`. See this blog post for more info: https://jasonet.co/posts/new-features-of-github-actions/#passing-data-to-future-steps
        asset_path: 'SophiApp\SophiAppCE\bin\Release\SophiAppCE.zip'
        asset_name: 'SophiAppCE.zip'
        asset_content_type: application/zip

